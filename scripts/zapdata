#!/usr/bin/env node

import fs from "node:fs";
import path from "node:path";
import process from "node:process";
import readline from "node:readline/promises";
import { fileURLToPath } from "node:url";
import { stdin as input, stdout as output } from "node:process";
import dotenv from "dotenv";
import mysql from "mysql2/promise";

const repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "..");
const envPath = path.join(repoRoot, ".env");

function printUsage() {
  console.error("Usage: ./scripts/zapdata [--yes] <user-email|ALL>");
  console.error("");
  console.error("Deletes only user training data while keeping login rows and non-training app data.");
}

function parseArgs(argv = []) {
  const args = [...argv];
  const force = args.includes("--yes");
  const positional = args.filter((entry) => entry !== "--yes");

  if (positional.length !== 1) {
    printUsage();
    process.exit(1);
  }

  return {
    force,
    target: String(positional[0] || "").trim()
  };
}

function loadEnv() {
  if (!fs.existsSync(envPath)) {
    throw new Error(`Missing .env at ${envPath}`);
  }

  const result = dotenv.config({
    path: envPath
  });
  if (result.error) {
    throw result.error;
  }

  const requiredKeys = ["DB_HOST", "DB_PORT", "DB_NAME", "DB_USER", "DB_PASSWORD"];
  const missingKeys = requiredKeys.filter((key) => !String(process.env[key] || "").trim());
  if (missingKeys.length > 0) {
    throw new Error(`Missing required .env keys: ${missingKeys.join(", ")}`);
  }
}

async function confirmDestructiveAction(targetLabel) {
  const rl = readline.createInterface({
    input,
    output
  });

  try {
    const answer = await rl.question(
      `Delete training data for ${targetLabel}? Type DELETE to continue: `
    );
    return String(answer || "").trim() === "DELETE";
  } finally {
    rl.close();
  }
}

async function deleteTrainingDataForUser(connection, { userId }, deletedCounts) {
  deletedCounts.workoutSetLogs += (
    await connection.query(
      "DELETE FROM workout_set_logs WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.workoutOccurrenceExercises += (
    await connection.query(
      "DELETE FROM workout_occurrence_exercises WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.userExerciseProgress += (
    await connection.query(
      "DELETE FROM user_exercise_progress WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.workoutOccurrences += (
    await connection.query(
      "DELETE FROM workout_occurrences WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.personalStepVariations += (
    await connection.query(
      "DELETE FROM personal_step_variations WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.userProgramAssignmentRevisions += (
    await connection.query(
      "DELETE FROM user_program_assignment_revisions WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.userProgramAssignments += (
    await connection.query(
      "DELETE FROM user_program_assignments WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.programScheduleEntries += (
    await connection.query(
      "DELETE FROM program_schedule_entries WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;

  deletedCounts.programs += (
    await connection.query(
      "DELETE FROM programs WHERE user_id = ?",
      [userId]
    )
  )[0].affectedRows;
}

async function main() {
  const { force, target } = parseArgs(process.argv.slice(2));
  loadEnv();
  const deleteAllUsers = target === "ALL";
  const confirmationLabel = deleteAllUsers ? "ALL users" : target;

  if (!force) {
    const confirmed = await confirmDestructiveAction(confirmationLabel);
    if (!confirmed) {
      console.error("Aborted.");
      process.exit(1);
    }
  }

  const connection = await mysql.createConnection({
    host: process.env.DB_HOST,
    port: Number(process.env.DB_PORT),
    database: process.env.DB_NAME,
    user: process.env.DB_USER,
    password: process.env.DB_PASSWORD
  });

  const deletedCounts = {
    workoutSetLogs: 0,
    workoutOccurrenceExercises: 0,
    workoutOccurrences: 0,
    userExerciseProgress: 0,
    personalStepVariations: 0,
    userProgramAssignmentRevisions: 0,
    userProgramAssignments: 0,
    programScheduleEntries: 0,
    programs: 0
  };

  try {
    await connection.beginTransaction();

    const [userRows] = await connection.query(
      deleteAllUsers
        ? "SELECT id, email FROM users ORDER BY id ASC"
        : "SELECT id, email FROM users WHERE email = ? LIMIT 1",
      deleteAllUsers ? [] : [target]
    );

    const normalizedUsers = Array.isArray(userRows)
      ? userRows
        .filter((row) => row?.id && row?.email)
        .map((row) => ({
          userId: Number(row.id),
          email: String(row.email || "").trim()
        }))
      : [];

    if (normalizedUsers.length < 1) {
      throw new Error(
        deleteAllUsers
          ? "No users found."
          : `No user found for email "${target}".`
      );
    }

    for (const user of normalizedUsers) {
      await deleteTrainingDataForUser(connection, user, deletedCounts);
    }

    await connection.commit();
  } catch (error) {
    await connection.rollback();
    throw error;
  } finally {
    await connection.end();
  }

  console.log(
    deleteAllUsers
      ? "zapdata complete for ALL users"
      : `zapdata complete for ${target}`
  );
  console.log(JSON.stringify(deletedCounts, null, 2));
  console.log("Login rows and non-training app data were left intact.");
}

main().catch((error) => {
  console.error(error instanceof Error ? error.message : String(error));
  process.exit(1);
});
